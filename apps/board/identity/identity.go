// Package identity contains Board identifiers shared by the application
// manifest and its adapters without creating parent/child package cycles.
package identity

const (
	AppID            = "aegis.board"
	PrimaryDataSpace = "primary"
)
