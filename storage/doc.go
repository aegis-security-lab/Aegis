// Package storage defines cross-runtime persistence contracts for execution
// events and large artifacts. Domain-owned persistence such as Coordination executions
// and agentcore Sessions remains in those domain packages.
package storage
