# Bundled shadcn skill

Vendored from the locally installed shadcn agent skill at
`~/.agents/skills/shadcn` for offline use by Alvax Studio website agents.

The complete text resources referenced by `SKILL.md` are bundled alongside it
and copied into each website workspace under `.pi/skills/shadcn/`.
